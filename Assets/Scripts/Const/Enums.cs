using UnityEngine;

namespace Const {
    public enum UILayer {
        BASIC_UI,
        OVER_LAY,
        TOP_UI,
    }

    public enum UIState {
        NORMAL,
        INIT,
        SHOW,
        HIDE,
    }

    public enum UiId {
        MainMenu,
        StartGame,
        NewGameWarning,
        Loading
    }

}